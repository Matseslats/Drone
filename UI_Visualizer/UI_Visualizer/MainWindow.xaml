<Window x:Class="UI_Visualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI_Visualizer"
        xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local:SceneViewModel x:Key="SceneViewModel" />
    </Window.Resources>
    <Grid>
		<!-- Left half of the screen -->
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- WebBrowser control for hosting the Leaflet map -->
        <esri:SceneView x:Name="MainSceneView"
                        Scene="{Binding Scene, Source={StaticResource SceneViewModel}}" />

        <!-- Add other controls to the right half of the screen if needed -->
		<TextBlock HorizontalAlignment="Left" Margin="50,56,0,0" TextWrapping="Wrap" Text="Select a message option and then choose the Display button." VerticalAlignment="Top" Grid.Column="1"/>
		<RadioButton x:Name="HelloButton" Content="Hello" IsChecked="True" HorizontalAlignment="Center" Margin="0,154,0,0" VerticalAlignment="Top" Grid.Column="1"/>
		<Button Content="Display" HorizontalAlignment="Center" Margin="0,271,0,0" VerticalAlignment="Top" Width="76" Click="Button_Click" Grid.Column="1"/>
	</Grid>

</Window>
